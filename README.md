# it-tools

A collection of useful IT tools built with Go and vanilla HTML/CSS/JS.

## 🚨 FOR AI AGENTS (MANDATORY) 🚨

**CRITICAL RULE 0: YOU MUST READ `AGENTS.md` BEFORE DOING ANYTHING ELSE.**

If you are an AI agent (like Blackbox, Claude, Cursor, etc.) assigned to work on this repository, **your very first action MUST be to read the [AGENTS.md](AGENTS.md) file in its entirety.** 

It contains strict, non-negotiable rules about:
- The Git Flow and PR process (You cannot merge directly).
- Branch naming conventions (`feature/{tool-id}`).
- The requirement to create `specs/{feature}/context.md` and `plan.md` before coding.
- Architectural and coding standards.

**DO NOT execute any commands, write any code, or start any task until you have read and understood `AGENTS.md`.**
